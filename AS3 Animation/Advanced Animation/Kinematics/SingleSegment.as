package {	import flash.display.Sprite;	import flash.events.Event;		public class SingleSegment extends Sprite {		private var segment:Segment;				public function SingleSegment():void {			init();		}				private function init():void {			segment = new Segment(100,20);			segment.x = stage.stageWidth/2;			segment.y = stage.stageHeight/2;			addChild(segment);			stage.addEventListener(Event.ENTER_FRAME, onStartFrame);		}				private function onStartFrame(evt:Event):void {			var dx:Number = mouseX - segment.x;			var dy:Number = mouseY - segment.y;			var angle:Number = Math.atan2(dy,dx);			segment.rotation = angle * 180 / Math.PI;						var segW:Number = segment.getPivot().x - segment.x;			var segH:Number = segment.getPivot().y - segment.y;			segment.x = mouseX - segW;			segment.y = mouseY - segH;		}	}}